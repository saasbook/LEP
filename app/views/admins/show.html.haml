%h1= "Admin #{@user.first_name}"
%h3= "ID #{@user.id}"

= link_to 'Show users', admins_path 
= link_to 'Show groups', groups_path, :id => 'show groups'

%h5= "Current Application Deadline: #{User::APPLICATION_DEADLINE}"

%h4= "Set Application Deadline"
= render 'set_app_deadline'