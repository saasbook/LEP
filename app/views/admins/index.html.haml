%h2 All Users
%h3 Admins
%table.table
  %thead
    %tr
      %th
        =link_to 'First name', admins_path(:sort => 'first_name')
      %th
        =link_to 'Last name', admins_path(:sort => 'last_name')
  %tbody
    - @admins.each do |admin|
      %tr
        %td= admin.first_name
        %td= admin.last_name
        %td= link_to "Revoke admin", admin_revoke_admin_path(:id => @user.id, :user_id => user.id, :authenticity_token => form_authenticity_token), :method => :put

= link_to 'Pair students', pairing_path(@user.id)
